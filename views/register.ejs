<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/register.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter&family=Josefin+Sans&family=Trocchi&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css" rel="stylesheet">

    <title>Registro</title>
</head>
<body>
    <header>
        <a href="/"><img class="logo" src="/img/Copia de Absolute Fashion.png" alt="logo"> </a>
    </header>
    <section class="seccion">
        <div class="div1"> 
            <p class="camposs">Registro</p>
            <p class="campos">Todos los campos son obligatorios</p>

            <form action="/register" method="POST" enctype="multipart/form-data">
        <div class="errors1">
            <div id="uno">
                <% if (errors.nombre) { %>
                    <div><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.nombre.msg %> </div>
                <% } else {
                    ''
                }%>
            </div>
            <div id="dos">
                <% if (errors.apellido) { %>
                    <div><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.apellido.msg %> </div>
                <% } else {
                    ''
                }%>
            </div>
        </div>
            <div class="nombreyapellido">
                <input type="text" name="nombre" placeholder="Nombre" value="<%= body.nombre ? body.nombre : '' %>">
                <input type="text" name="apellido" placeholder="Apellido" value="<%= body.apellido ? body.apellido : '' %>">
            </div>
            <div class="errors1">
                <div>
                    <% if (errors.email) { %>
                        <div><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.email.msg %> </div>
                    <% } else {
                        ''
                    }%>
                </div>
            </div>
            <div>
                <input class="inputmail" type="email" name="email" placeholder="Mail" value="<%= body.email ? body.email : '' %> ">
            </div>
            <div class="errors1">
                    <% if (errors.contraseña && !errors.confirmarla) { %>
                        <div><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.contraseña.msg %> </div>
                    <% } else if (errors.confirmarla && !errors.contraseña) { %>
                        <div><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.confirmarla.msg %> </div>
                    <% } else if (errors.contraseña && errors.confirmarla) {%>
                        <div id="errors4"><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.contraseña.msg %> </div>
                        <div id="errors3"><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.confirmarla.msg %> </div>
                        <% } %>
            </div>
            <div class="contras">
                <input type="password" name="contraseña" placeholder="Contraseña">
                <input type="password" name="confirmarla" placeholder="Confirmar contraseña">
            </div>
            <div class="errors1">
                <div>
                    <% if (errors.imagen) { %>
                        <div><i style="color: red;" class="fas fa-exclamation"></i> <%= errors.imagen.msg %> </div>
                    <% } else {
                        ''
                    }%>
                </div>
            </div>
        <div class="imagen">
            <label for="">Foto de perfil (opcional)</label>
            <input type="file" name="imagen">
        </div>
    </div>
        <div class="terminos"><p>Al hacer clic en crear cuenta, aceptás las <a href="">Condiciones de uso y la Política de privacidad</a> de Dharma.</p></div>
    <div class="otros">
        <div>
            <div class="divcrearcuenta">
                    <input class="crearcuenta" type="submit" value="Crear cuenta">
            </div>
        </form>
            <div class="grid2">
                    <p class="registrado">¿Ya estás registrado?</p>
                    <a class="iniciala" href="/login">Inicia sesión <i class="fas fa-sign-in-alt"></i></a>
            </div>
        </div>
            <div class="losdos">
                <a class="guglyfeis" href="">Ingresar con <i class="fab fa-google"></i>oogle</a>

                <a class="guglyfeis" href="">Ingresar con <i class="fab fa-facebook-f"></i>acebook</a>
            </div>
        </div>
    </div>
    </section>

    <%- include('partials/footer'); %>